Update a litebrite item.

Usage: /lb-update <id> [--title <title>] [--status <status>] [-t <type>] [-p <priority>] [-d <description>] [--parent <id>]

Run `lb update <id>` with the provided flags.
Then run `lb show <id>` to confirm the changes.
